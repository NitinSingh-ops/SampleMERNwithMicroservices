apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-mern-chart.fullname" . }}
  labels:
    {{- include "sample-mern-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-mern-chart.selectorLabels" . | nindent 4 }}
